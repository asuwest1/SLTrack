# =============================================================================
# SLTrack - Environment Configuration
# =============================================================================
# Copy this file to .env and update values for your environment.

# Database Type: 'sqlite' (development) or 'mssql' (production SQL Server)
DB_TYPE=sqlite

# ---------- SQLite Settings (only when DB_TYPE=sqlite) ----------
# DB_PATH=./sltrack.db

# ---------- SQL Server Settings (only when DB_TYPE=mssql) ----------
# DB_SERVER=your-sql-server.company.com
# DB_NAME=SLTrack
# DB_PORT=1433
# DB_ENCRYPT=true
# DB_TRUST_CERT=false

# SQL Server Authentication (choose one method):

# Option A: SQL Authentication
# DB_USER=sltrack_app
# DB_PASSWORD=your_secure_password

# Option B: Windows Integrated Authentication
# DB_WINDOWS_AUTH=true

# ---------- Server Settings ----------
PORT=3001
NODE_ENV=development
CORS_ORIGINS=http://localhost:3000,http://localhost:3001
